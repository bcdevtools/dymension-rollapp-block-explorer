export const DEFAULT_DATE_TIME_FORMAT = 'YYYY-MM-DD HH:mm:ss +UTC';

export const APP_NAME = 'Dr.BE';

export const ADDRESS_SUMMARY_COINS_PAGE_SIZE = 10;

export const DEFAULT_PAGINATION_SIZE = 25;

export const MAX_PAGINATION_SIZE = 100;

export const PAGE_SIZE_PARAM_NAME = 'ps';
export const PAGE_PARAM_NAME = 'p';

export const SEARCH_PLACEHOLDER = 'Rollapp, Address, Block, Transaction hash, etc.';

export const enum ChainType {
  EVM = 'evm',
  WASM = 'wasm',
}

export const DEFAULT_CACHE_DURATION = 10;
